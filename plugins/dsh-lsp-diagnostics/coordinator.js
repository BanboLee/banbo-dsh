/**
 * Diagnostics coordinator: owns the post-execute augment transaction, the
 * operation registry/controller, and the retired late-final-stat registry.
 *
 * Todo 1 skeleton: the real three-parameter `tools/post-execute` listener,
 * admission/abort/await ownership, and the one-shot final commit gate are
 * implemented in Todo 5.
 *
 * @module dsh-lsp-diagnostics/coordinator
 */

/**
 * Create the diagnostics coordinator.
 * @param {unknown} _options - collector/runtime/config/fs dependencies.
 * @returns {never} - implemented in Todo 5.
 */
export function createDiagnosticsCoordinator(_options) {
  throw new Error('dsh-lsp-diagnostics: coordinator is implemented in Todo 5')
}
