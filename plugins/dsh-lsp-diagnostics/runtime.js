/**
 * Diagnostics runtime: per-provider/workspace LSP sessions over subprocesses.
 *
 * Todo 1 skeleton: the full JSON-RPC state machine, bounded read, correlation,
 * eviction/restart, and the unique graceful-first teardown are implemented in
 * Todo 3.
 *
 * @module dsh-lsp-diagnostics/runtime
 */

/**
 * DiagnosticsRuntime skeleton.
 */
export class DiagnosticsRuntime {
  constructor() {
    throw new Error('dsh-lsp-diagnostics: diagnostics runtime is implemented in Todo 3')
  }

  /**
   * @returns {never} - implemented in Todo 3.
   */
  diagnose() {
    throw new Error('dsh-lsp-diagnostics: diagnostics runtime is implemented in Todo 3')
  }

  /**
   * @returns {never} - implemented in Todo 3.
   */
  stopAdmission() {
    throw new Error('dsh-lsp-diagnostics: diagnostics runtime is implemented in Todo 3')
  }

  /**
   * @returns {never} - implemented in Todo 3.
   */
  dispose() {
    throw new Error('dsh-lsp-diagnostics: diagnostics runtime is implemented in Todo 3')
  }
}
