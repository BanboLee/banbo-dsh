/**
 * Mutation collector for `fs/observed` write/edit observations.
 *
 * Todo 1 skeleton: the collector behavior (pending-by-exec candidates, the
 * independent per-target monotonic generation counter, and the active marker
 * with `take`/`isCurrent`/`retireIfCurrent`) is implemented in Todo 2.
 *
 * @module dsh-lsp-diagnostics/collector
 */

/**
 * Create a mutation collector.
 * @returns {never} - implemented in Todo 2.
 */
export function createMutationCollector() {
  throw new Error('dsh-lsp-diagnostics: mutation collector is implemented in Todo 2')
}
