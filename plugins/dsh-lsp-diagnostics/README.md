# dsh-lsp-diagnostics

Host-plane Cordis bundle plugin for DeepSeek Harness: after an official
`write`/`edit`/`str_replace_editor` mutation lands inside the session
workspace, the tool result still succeeds and the next model inference carries
a single persistent LSP diagnostics plugin notice.

> **Status**: Todo 1 scaffold. This README is a chapter skeleton; the full
> contract is completed in a later todo and enforced by the implementation
> plan at `docs/plan/dsh-lsp-diagnostics-v1-implementation-plan.md`.

## Usage

(Completed in a later todo — install command and profile wiring.)

## Config

(Completed in a later todo — strict validator, defaults, and the closed
`.ts`/`.tsx`/`.go` extension route.)

## Behavior

(Completed in a later todo — eligibility, bounded reads, correlation,
freshness, aggregate rendering, and fail-open boundaries.)

## Model Experience

(Completed in a later todo — the single aggregate notice contract.)

## Known Limitations and Deferred Work

(Completed in a later todo — shell/redirect bypasses, agentless execution,
workspace-less sessions, and deferred diagnostics are out of scope.)

## Verification

(Completed in a later todo — unit/fixture/composition test commands.)
