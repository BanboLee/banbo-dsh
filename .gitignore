node_modules/
*.log
research/
dist/
# Local-only agent execution evidence (plans, task logs, review reports).
# Never committed; run the plan/evidence verifiers manually on the authoring machine.
/.omo
